type Wpis = record {
    name: text;
    content: text;
};

service : {
    "greet": (text, int8) -> (text) query;
    "dodaj_wpis": (text, text) -> ();
    "odczytaj_wpisy": () -> (vec Wpis) query;
};
